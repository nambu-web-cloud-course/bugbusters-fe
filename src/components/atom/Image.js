// import styled, { css } from "styled-components";

// const sizeStyles = css`
//   ${({ theme, size }) => css`
//     width: ${theme.img.size[size] || theme.img.size.sm};
//     height: ${theme.img.size[size] || theme.img.size.sm};
//   `}
// `;

// const fullWidthStyle = css`
//   ${(props) =>
//     props.fullWidth &&
//     css`
//       width: 100%;
//       justify-content: center;
//     `}
// `;

// const StyledImage = styled.img`
//   /* Common styles */
//   border-radius: 0.25rem 
//   ${sizeStyles} 
//   ${fullWidthStyle};
// `;

// export default function Image({ size, fullWidth, ...rest }) {
//   return <StyledImage size={size} fullWidth={fullWidth} {...rest} />;
// }
